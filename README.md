# Vita Design - client preview

Rendered output only. This repository exists solely to serve a preview build via
GitHub Pages; the source, build pipeline and original assets are private.

Every page is served `noindex, nofollow`: this is a preview for review, not a
launched site. Product renderings shown here are supplied by a manufacturing
partner and are labelled as such on every page where they appear.
